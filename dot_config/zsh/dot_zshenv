# Set the path variable
typeset -U path PATH
path=(~/.local/bin $path)
export PATH

# For a colored sudo prompt
export SUDO_PROMPT="$(tput setaf 1 bold)[sudo]$(tput sgr0) $(tput setaf 3)password for$(tput sgr0) $(tput setaf 5)%p$(tput sgr0): "

# For java home
export JAVA_HOME=/usr/lib/jvm/default
